services:
  - type: web
    name: markitdown-service
    runtime: docker
    rootDir: services/markitdown-service
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    plan: free
    envVars:
      - key: MARKITDOWN_SERVICE_TOKEN
        sync: false
